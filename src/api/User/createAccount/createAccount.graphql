type Mutation {
  createAccount(
    userId: String!
    password: String!
    nickname: String!
    dateOfBirth: Int!
    gender: String!
    useTermsPrivacyagreement: Boolean!
    bio: String
  ): String!
}

type Query {
  somthing: String
}
