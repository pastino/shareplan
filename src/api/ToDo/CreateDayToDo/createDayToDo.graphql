type Mutation {
  createDayToDo(
    toDoList: String!
    color: String!
    startDate: String!
    startTime: String
    endDate: String!
    endTime: String
    alrams: [Int!]
    categoryId: [Int!]
    memo: String
    goalId: String
    today: String!
  ): DayToDo!
}
