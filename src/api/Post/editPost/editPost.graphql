type Mutation {
  editPost(
    id: String!
    caption: String
    title: String
    postPrivate: Boolean
    goalId: String
    toDoId: String
    files: [String!]
    postRatio: [Float!]
  ): DayToDo!
}
