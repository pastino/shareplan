type Mutation {
  editGoalCard(
    goalId: String!
    goalText: String
    dDay: String
    startDate: String
    keyWord: String
    category: String
    detailCategory: String
    cardColor: String
    cardPrivate: Boolean
  ): Goal!
}
